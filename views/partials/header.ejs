<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= typeof title !== 'undefined' ? title : 'KLE Canteen' %> | KLE Canteen</title>

  <!-- Tailwind v4 compiled output -->
  <link rel="stylesheet" href="/css/output.css" />

  <!-- Font Awesome 6 icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" crossorigin="anonymous" />

  <!-- Socket.io client -->
  <script src="/socket.io/socket.io.js"></script>

  <meta name="theme-color" content="#f97316" />
  <meta name="description" content="KLE Campus Canteen Pre-Order System" />
</head>
<!--
  NOTE for Tailwind v4:
  - font-body / font-display are set via CSS var(--font-body) in @layer base
  - bg-amber-50 applied via body in @layer base
  - No need for class="font-body" on body tag
-->
<body class="min-h-screen flex flex-col">
